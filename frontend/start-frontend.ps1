# Attiva l'ambiente Node.js (se necessario)
# (opzionale: puoi usare nvm o nodenv se gestisci più versioni)

# Installa le dipendenze (solo la prima volta o se cambiano)
npm install

# Avvia il server di sviluppo Vite
npm run dev
